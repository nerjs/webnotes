<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= error.message %></title>
</head>
<body class="error error-<%= String(error.code)[0] %>xx error-<%= error.code %>">

    <a href="/" class="home">Home</a>

    <div class="container">
        <h2><%= error.message %></h2>
        <% if (error.code >= 400) { %>
            <h1>
                <% if (error.code >= 400 && error.code < 500) { %>
                    Client Error
                <% } else { %>
                    Server Error
                <% } %>
            </h1>
        <% } %>
        <% if (canStack) { %>
            <pre class="stacktrace">
                <%= error.stack %>
            </pre>
        <% } %>
    </div>


<style>
    body {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
    }

    .home {
        text-decoration: none;
        font-size: 20px;
        font-weight: bold;
        color: #202573;
        position: fixed;
        top: 20px;
        left: 20px;
        z-index: 2;
    }

    .container {
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        display: flex;
        justify-content: start;
        align-items: center;
        flex-direction: column;
        z-index: 1;
    }
    .stacktrace {
        width: 90%;
        margin: 5px auto;
        padding: 10px;
        box-shadow: 0 0 5px #999;
    }

    h1 {
        margin: 5px 10px 10px;
        font-size: 24px;
        color: #000;
    }

    h2 {
        margin: 15px 10px 0;
        font-size: 14px;
        color: #0006;
    }
</style>    
</body>
</html>